title: Статус
new-page: true
is-special: true
on-arrival: {!
var electionDate = new Date(1996, 5, 16);
var currentDate = new Date(Q.year, Q.month - 1, Q.day);
Q.days_to_election = Math.floor((electionDate - currentDate) / (1000 * 60 * 60 * 24));
Q.yelt_disp = Q.yeltsin_support.toFixed(1);
Q.zyug_disp = Q.zyuganov_support.toFixed(1);
!}

= Дашборд кампании-96

**Дата:** {! Q.day !} {! Q.month == 1 ? 'января' : Q.month == 2 ? 'февраля' : Q.month == 3 ? 'марта' : Q.month == 4 ? 'апреля' : Q.month == 5 ? 'мая' : Q.month == 6 ? 'июня' : 'июля' !} {! Q.year !} г.
**До первого тура:** {! Q.days_to_election !} дн.

**Ельцин:** [+ yelt_disp +]%
**Зюганов:** [+ zyug_disp +]%

- @polls_map: Карта регионов
- @main: Вернуться

@polls_map
new-page: true

= Срез по регионам (Зюганов / Ельцин)

| Регион | Поддержка |
| :--- | :--- |
| **Москва** | {! Q.region_data.Moscow.zyug.toFixed(1) !}% / {! Q.region_data.Moscow.yelt.toFixed(1) !}% |
| **Кузбасс** | {! Q.region_data.Kemerovskaya.zyug.toFixed(1) !}% / {! Q.region_data.Kemerovskaya.yelt.toFixed(1) !}% |

- @status: Назад
